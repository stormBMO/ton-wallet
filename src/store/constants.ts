import { Network } from "./types";

export const TESTNET_NETWORK: Network = 'testnet';
export const MAINNET_NETWORK: Network = 'mainnet';


export const SUPPORTED_NETWORKS: Network[] = [
    TESTNET_NETWORK,
    MAINNET_NETWORK,
];

export const DEFAULT_NETWORK: Network = TESTNET_NETWORK;
